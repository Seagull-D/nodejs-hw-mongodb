# swagger/components/responses/401.yaml

description: Unauthorized error
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: '\"name\" is required. \"phoneNumber\" is required. \"email\" is required'
